let PDFMerger;_0ed‍.x([["MergePdfs",()=>MergePdfs]]);_0ed‍.w("pdf-merger-js",[["default",["PDFMerger"],function(v){PDFMerger=v}]]);// merge.js


       const MergePdfs = async (p1, p2) => {
  const merger = new PDFMerger();
  await merger.add(p1); // Merge all pages. Parameter is the path to file and filename.
  await merger.add(p2); // Merge only page 2

  await merger.save("public/merged.pdf"); // Save under given name and reset the internal document

  // Export the merged PDF as a Node.js Buffer
  // const mergedPdfBuffer = await merger.saveAsBuffer();
  // fs.writeSync('merged.pdf', mergedPdfBuffer);
};
