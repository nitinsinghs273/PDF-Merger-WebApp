let express,path,multer,fileURLToPath,MergePdfs;_830‍.w("express",[["default",["express"],function(v){express=v}]]);_830‍.w("path",[["default",["path"],function(v){path=v}]]);_830‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_830‍.w("url",[["fileURLToPath",["fileURLToPath"],function(v){fileURLToPath=v}]]);_830‍.w("./merge.js",[["MergePdfs",["MergePdfs"],function(v){MergePdfs=v}]]);





const __filename = fileURLToPath(_830‍._.meta.url);
const __dirname = path.dirname(__filename);

const app = express();
const port = 3000;

const upload = multer({ dest: "uploads/" });

// Middleware
app.use("/static", express.static("public"));

app.get("/", (req, res) => {
  res.sendFile(path.join(__dirname, "/template/index.html"));
});

app.post("/merge", upload.array("pdfs", 2), async (req, res, next) => {
  _830‍.g.console.log(req.files);
  const file1Path = path.join(__dirname, req.files[0].path);
  const file2Path = path.join(__dirname, req.files[1].path);

  await MergePdfs(file1Path, file2Path);
  res.redirect("/static/merged.pdf");
});

app.listen(port, () => {
  console.log(`Example app listening on port http://localhost:${port}`);
});
